// =========================================================
//  ProtectedRoute — Open access for instant learners
// =========================================================

export default function ProtectedRoute({ children }) {
  // Login removed: all pages are accessible to learners directly
  return children;
}
